import {makeStyles} from "@material-ui/core";

const useStyles = makeStyles((theme) => ({
    input: {
        width: '100%',
        marginTop: '15px'
    },
    inputSm: {
        width: '48%',
        marginTop: '15px'
    },
}));

export default useStyles